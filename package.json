{
  "name": "chat-editor-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "serve -s dist -l $PORT",
    "postinstall": "npm install --only=dev"  
  },
  "dependencies": {
    "axios": "^1.8.2",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.2.2"
  }
}
